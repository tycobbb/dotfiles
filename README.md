# dotfiles

install by runnning the install script:

```sh
$ ./install
```

this should:

- symlink all the dotfiles into your home directory
- install brew
- install any bundled brew packages
- install any vim plugins
